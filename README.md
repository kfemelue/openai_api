# AI Chatbot Application
This repository contains two simple applications. 

The `client` directory contains a HTML/CSS/JS chatbot user interface. 

The `server` directory contains a FastAPI Python application that makes external API calls to an AI Model.


The Chatbot Client is deployed [here]()
